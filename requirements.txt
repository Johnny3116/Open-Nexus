# Phase 0 skeleton runtime deps. For a full install with channels/senses,
# prefer `pip install -e .[anthropic,supabase,telegram,...]` (see pyproject.toml).
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
